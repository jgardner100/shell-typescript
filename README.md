This is a starting point for a TypeScript based
["Shell"](https://www.gardner.link).

1. Ensure you have `bun (1.3)` installed locally
1. Run `./jsh.sh` to run your program, which is implemented in
   `app/main.ts`.
